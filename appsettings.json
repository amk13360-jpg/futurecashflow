{
  "_SECURITY_NOTE": "This file should only contain non-sensitive configuration. Sensitive values must be set via environment variables or Azure Key Vault.",
  "DB_PORT": "3306",
  "NODE_ENV": "production",
  "_REQUIRED_ENV_VARS": [
    "DB_HOST",
    "DB_NAME", 
    "DB_USER",
    "DB_PASSWORD",
    "JWT_SECRET",
    "NEXTAUTH_URL",
    "AZURE_COMMUNICATION_CONNECTION_STRING",
    "AZURE_COMMUNICATION_SENDER"
  ]
}
